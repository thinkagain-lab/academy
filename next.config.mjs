/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['raw.githubusercontent.com'],
    },
    basePath: '/academy', // Replace 'reponame' with your actual repository name
  };
  
  export default nextConfig;
  